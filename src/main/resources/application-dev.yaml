datasource:
  host: 'localhost:5432'
  username: user
  password: password
  schema: public
  database: db

spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  level:
    org:
      springframework:
        web: DEBUG
        security: DEBUG

      pegasus:
        backendapi: INFO

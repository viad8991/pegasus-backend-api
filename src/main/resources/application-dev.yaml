datasource:
  host: 'localhost:5432'
  username: user
  password: password
  schema: public
  database: db

spring:
  jpa:
    show-sql: true

logging:
  level:
    org:
      springframework:
        web: INFO
        security: DEBUG

      pegasus:
        backendapi: INFO
